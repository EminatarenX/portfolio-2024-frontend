/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     domains: ["portfolio-eminataren.s3.us-east-1.amazonaws.com"]
    // }
    images: {
        unoptimized: true,
    },

};

export default nextConfig;
